# Discord (py-cord fork — provides sinks, start_recording)
py-cord[voice]>=2.6.0

# HuggingFace — STT (Whisper) + TTS (SpeechT5)
transformers>=4.40.0
torch>=2.2.0
torchaudio>=2.2.0
accelerate>=0.27.0
datasets>=2.18.0
soundfile>=0.12.1
librosa>=0.10.1

# Ollama (local LLM — Qwen3:32b)
ollama>=0.3.0

# Voice Activity Detection
webrtcvad>=2.0.10

# Env management
python-dotenv>=1.0.0

# Image processing (for Discord uploads)
Pillow>=10.0.0

# vault-writer (Phase 1 of Obsidian vault integration)
watchdog>=4.0
httpx>=0.28.1
pyyaml>=6.0
sqlite-vec>=0.1.6

# ai-team-gateway (unified Windows/Android client backend)
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=13.0
python-multipart>=0.0.9

# tests
pytest>=8.0
pytest-asyncio>=0.24
httpx-ws>=0.6
python-docx
python-pptx
